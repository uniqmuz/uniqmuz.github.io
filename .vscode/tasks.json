{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "dev",
            "type": "shell",
            "command": "bash -c 'bundle exec jekyll serve --watch --livereload --drafts'", //--no-watch --force_polling
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}